apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "myapp.fullname" . }}-db-configmap
  labels:
  {{- include "myapp.labels" . | nindent 4 }}
data:
  db_host: {{ .Values.dbConfigmap.dbHost | quote }}
  db_name: {{ .Values.dbConfigmap.dbName | quote }}