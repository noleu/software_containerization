apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    component: database
  name: db-pvc-claim0
  namespace: event-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
status: {}
