apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: managed-cert
spec:
  ingressClassName: nginx
  rules:
  - host: myeventsapp.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-cluster-ip-service
            port: 
              number: 80
  tls:
  - hosts:
    - myeventsapp.com
    secretName: myingress-cert